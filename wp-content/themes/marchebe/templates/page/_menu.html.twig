{% if children|length > 0 %}
    <nav aria-label="Filtrer les articles par catégorie"
         class="mx-auto max-w-5xl px-4 py-6 bg-white border-y border-gray-100"
         id="category-filter-menu">
        <ul class="flex flex-wrap justify-center gap-3">
            {% for item in children %}
                <li class="my-2">
                    <a class="px-4 py-2 rounded-full text-sm font-medium transition-all duration-200 border focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-cta-dark"
                       class="bg-white text-gray-700 border-gray-200 hover:border-cta-light hover:text-cta-dark"
                       href="{{ item.url }}"
                       aria-current="page">
                        {{ name ?? item.name }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endif %}