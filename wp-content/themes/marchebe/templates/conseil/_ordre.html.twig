<ul class="list-group">
    {% for ordre in ordres %}
        <li class="list-group-item">
            <a href="/wp-content/uploads/{{ ordre.file_name }}"
               title="Télécharger l'ordre" target="_blank">
                {{ ordre.name }}
            </a>
        </li>
    {% endfor %}
</ul>
