{% if images is defined and images|length > 0 %}
    <div class="grid grid-cols-2 md:grid-cols-3 gap-4 mt-6">
        {% for image in images %}
            <a href="https://bottin.marche.be/storage/{{ image.file_name }}"
               target="_blank"
               rel="noopener noreferrer"
               class="overflow-hidden rounded-lg hover:opacity-90 transition-opacity">
                <img src="https://bottin.marche.be/storage/{{ image.file_name }}"
                     alt="Image {{ loop.index }} - {{ fiche.company|default('commerce') }}"
                     class="w-full h-full object-cover"
                     loading="lazy"/>
            </a>
        {% endfor %}
    </div>
{% endif %}