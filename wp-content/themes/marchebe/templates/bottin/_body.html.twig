<div class="p-6 md:p-8">
    <div class="space-y-4">

        {% if fiche.comment1 %}
            <p class="text-gray-500 dark:text-gray-400 mt-1">
                {{ fiche.comment1 }}
            </p>
        {% endif %}

        {% if fiche.comment1 %}
            <p class="text-gray-500 dark:text-gray-400 mt-1">
                {{ fiche.comment3 }}
            </p>
        {% endif %}

        {% if fiche.comment1 %}
            <p class="text-gray-500 dark:text-gray-400 mt-1">
                {{ fiche.comment2 }}
            </p>
        {% endif %}

        {% include '@AcMarche/bottin/_address.html.twig' %}
        {% include '@AcMarche/bottin/_contact.html.twig' %}

        {% if bottinCategories is defined and bottinCategories|length > 0 %}
            <div class="mt-4">
                <div class="flex flex-wrap gap-2 mt-2">
                    {% for category in bottinCategories %}
                        <a href="{{ category.url }}"
                           class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-slate-100 text-cta-brown hover:bg-slate-200 transition-colors">
                            {{ category.name }}
                        </a>
                    {% endfor %}
                </div>
            </div>
        {% endif %}

        {% if bottinTags is defined and bottinTags|length > 0 %}
            <div class="mt-4">
                <div class="flex flex-wrap gap-2 mt-2">
                    {% for tag in bottinTags %}
                        <a href="{{ tag.url }}"
                           class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-teal-100 text-teal-800 dark:bg-teal-900 dark:text-teal-200 hover:bg-teal-200 dark:hover:bg-teal-800 transition-colors">
                            {{ tag.name }}
                        </a>
                    {% endfor %}
                </div>
            </div>
        {% endif %}

        {% include '@AcMarche/bottin/_hours.html.twig' %}
        {% include '@AcMarche/bottin/_images.html.twig' %}

    </div>
    {% include '@AcMarche/bottin/_social_networks.html.twig' %}
</div>